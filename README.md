# ForumAttritionErrorWeb
ForoAttritionErrorWeb is a lightweight ASP.NET Core Razor Pages forum app where users can register, log in, create posts, and reply by category. It is designed to run even without a configured database connection, showing a clear message when data access is unavailable.
